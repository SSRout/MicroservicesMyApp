// Environment configuration for production
export const environment = {
  production: true,
  apiUrl: "/api",
  apiEndpoints: {
    catalog: "/Catalog",
    cart: "/Cart",
    order: "/Order",
  },
};
